import {test} from 'node:test';import assert from 'node:assert/strict';import {validateUpload,safeXml,legacySlides} from '../src/lib/documents.js';import {CFB} from 'xlsx';
test('supported formats and size checks reject unsupported or legacy DOC explicitly',()=>{for(const ext of ['pdf','docx','xls','xlsx','ppt','pptx','jpg','jpeg','png','gif','giff','xml','csv','txt','md','json','webp','bmp','tsv'])assert.equal(validateUpload({name:`study.${ext}`,size:100}),ext);assert.throws(()=>validateUpload({name:'study.doc',size:1}),/DOCX or PDF/);assert.throws(()=>validateUpload({name:'run.exe',size:1}),/Unsupported/);assert.throws(()=>validateUpload({name:'huge.xlsx',size:21*1024*1024}),/20 MB/);});
test('XML entities are rejected before parsing and damaged legacy PPT fails clearly',()=>{assert.throws(()=>safeXml('<!DOCTYPE a><a/>'),/entities/);assert.throws(()=>safeXml('<!ENTITY a "b"><a/>'),/entities/);const file=CFB.utils.cfb_new();CFB.utils.cfb_add(file,'PowerPoint Document',new Uint8Array([15,0,238,3,255,255,255,255]));assert.throws(()=>legacySlides(CFB.write(file,{type:'buffer'}),CFB),/Damaged/);});
