import {test,expect} from '@playwright/test';
test('animated flow supports play, pause, manual steps and replay without running external tools',async({page})=>{let calls=0;await page.route('**/api/study',r=>{calls++;return r.abort();});await page.goto('/');await page.getByRole('tab',{name:'Behind the RAG'}).click();const flow=page.getByRole('region',{name:'Animated RAG walkthrough'});await expect(flow).toHaveAttribute('data-playing','false');await expect(flow.locator('.flow-node')).toHaveCount(7);await flow.getByLabel('Walkthrough playback speed').selectOption('1100');await flow.getByRole('button',{name:'Play walkthrough'}).click();await expect(flow.getByRole('progressbar')).toHaveAttribute('aria-valuenow','2');await flow.getByRole('button',{name:'Pause walkthrough'}).click();const paused=await flow.getByRole('progressbar').getAttribute('aria-valuenow');await page.waitForTimeout(1300);await expect(flow.getByRole('progressbar')).toHaveAttribute('aria-valuenow',paused);await flow.getByRole('button',{name:'Inspect step 3: Recognize printed scans'}).click();await expect(flow.getByRole('article',{name:'Selected flow step details'})).toContainText('Tesseract.js');await flow.getByRole('button',{name:'Next step'}).click();await expect(flow.getByRole('progressbar')).toHaveAttribute('aria-valuenow','4');await flow.getByRole('button',{name:'Inspect step 7: Reuse the session index'}).click();await expect(flow.getByRole('button',{name:'Next step'})).toBeDisabled();await flow.getByRole('button',{name:'Replay from start'}).click();await expect(flow.getByRole('progressbar')).toHaveAttribute('aria-valuenow','1');await flow.getByRole('button',{name:'MCP & development',exact:true}).click();await expect(flow).toContainText('student uploads and questions do not call GitHub MCP');await flow.getByRole('button',{name:'Inspect step 3: Read external project state'}).click();await expect(flow.getByRole('article',{name:'Selected flow step details'})).toContainText('Model Context Protocol');await flow.getByRole('button',{name:'Study materials',exact:true}).click();await flow.getByRole('button',{name:'Inspect step 2: Cover readable sources'}).click();await expect(flow.locator('.flow-detail')).toContainText('4 passages considered');expect(calls).toBe(0);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);});
test('question path explains retrieval, current evidence and actual response mode',async({page})=>{await page.goto('/');await page.getByRole('button',{name:'Ask this document',exact:true}).click();await page.getByLabel('Question',{exact:true}).fill('electric charge');await page.getByRole('button',{name:'Find an answer'}).click();await expect(page.getByRole('heading',{name:'Demo / source-based answer',level:2})).toBeVisible();await page.getByRole('tab',{name:'Behind the RAG'}).click();const flow=page.getByRole('region',{name:'Animated RAG walkthrough'});await flow.getByRole('button',{name:'Question answering',exact:true}).click();await expect(flow.locator('.flow-node')).toHaveCount(6);await flow.getByRole('button',{name:'Inspect step 3: Find relevant passages'}).click();await expect(flow.locator('.flow-detail')).toContainText('selected passages');await flow.getByRole('button',{name:'Inspect step 5: Create the response'}).click();await expect(flow.locator('.flow-detail')).toContainText('Source selection · no LLM call');await flow.getByText('Inspect a real passage from this session').click();await expect(flow.locator('.flow-context blockquote')).toContainText('charge');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);});
test('reduced motion keeps flow manual by default and removes moving effects',async({page})=>{await page.emulateMedia({reducedMotion:'reduce'});await page.goto('/');await page.getByRole('tab',{name:'Behind the RAG'}).click();const flow=page.getByRole('region',{name:'Animated RAG walkthrough'});await expect(flow).toHaveAttribute('data-reduced-motion','true');await expect(flow).toHaveAttribute('data-playing','false');await flow.getByRole('button',{name:'Next step'}).click();await expect(flow.getByRole('progressbar')).toHaveAttribute('aria-valuenow','2');await flow.getByRole('button',{name:'Play walkthrough'}).click();const animation=await flow.locator('.flow-active .flow-node-icon').evaluate(e=>getComputedStyle(e).animationName);expect(animation).toBe('none');await flow.getByRole('button',{name:'Pause walkthrough'}).click();await expect(flow).toContainText('Reduced motion is enabled');});
